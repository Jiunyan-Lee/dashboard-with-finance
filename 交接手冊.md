# 個人資產管理系統：Codex 交接手冊

> 更新日期：2026-09-14  
> 目的：讓沒有前序對話的新 Codex 任務安全接手並繼續開發。

## 1. 新任務第一則指令

可將以下內容交給新的 Codex 任務：

> 請先完整閱讀 `README.md`、`交接手冊.md` 與 `目前版本說明.md`，再檢查 `index.html`、`app.js`、`styles.css`。這個專案的 `app.js` 有多個同名函式，最後一個 function declaration 才會生效；修改前必須使用 `rg -n "^function 函式名稱" app.js` 找到最後定義。請保留使用者既有資料與無關修改，先以匿名測試 JSON 驗證，不要上傳真實財務資料。

## 2. 專案位置與啟動

- 專案：`G:\我的雲端硬碟\Codex work\個人資產管理系統`
- 入口：`index.html`
- 程式：`app.js`
- 樣式：`styles.css`
- 圖表：Chart.js CDN
- 建置流程：無；不需要 npm 或伺服器
- 資料儲存：瀏覽器 Local Storage

直接以瀏覽器開啟 `index.html` 即可。圖表函式庫由 CDN 載入，首次開啟需要網路。

## 3. 文件分工與閱讀順序

1. `README.md`：快速了解用途、啟動方式、備份與文件入口。
2. `交接手冊.md`：接手步驟、修改規則、有效覆寫與驗證方式。
3. `目前版本說明.md`：資料模型、完整公式、頁面規格、圖表行為與回歸清單。
4. `上線架構與建置流程.md`：尚未實作的 Netlify／Supabase 雲端化規劃。

若文件與程式不一致，先以瀏覽器實際行為與 `app.js` 最後生效函式交叉確認，不要直接假設較前面的函式有效。

## 4. 修改前安全檢查

- 先閱讀使用者最新需求，尊重「先不要修改」與限定頁面的範圍。
- 不要修改需求未涵蓋的分頁；總覽與動態回顧的圖表邏輯彼此獨立。
- 不要刪除、覆寫或提交真實 JSON 備份、截圖、帳密與財務資料。
- 修改資料結構、匯入流程或 Local Storage 前，先提醒使用者匯出備份。
- 工作區可能已有使用者變更；不要用 `git reset --hard`、`git checkout --` 等破壞性操作。
- 此專案目前不是以正式重構架構維護；小型需求優先做局部且可回復的修改。

## 5. 重要架構特性

### 同名函式覆寫

`app.js` 歷史上以檔案尾端重新宣告同名函式的方式演進。JavaScript 會採用最後一個 function declaration。每次修改前先執行：

```powershell
rg -n "^function renderDashboard" app.js
rg -n "^function incomeExpenseMetricsV13" app.js
```

不要修改前段已被覆寫的版本，否則頁面可能完全沒有變化。若新增臨時覆寫，需加上下一個 Version 註解並同步更新版本說明；長期方向仍是重構為模組。

### 目前關鍵有效函式

- `renderRecord`：月度資料卡 HTML；檔案前段有歷史同名定義，必須以 `rg` 找到最後一個定義後再修改。
- `renderDashboard`：最後定義為 Version 95。
- `initialAssetsV99`：取得該月份勾選的銀行初始資產；未勾選即為 0。
- `spendingValuesV88`：最後定義為 Version 99，供累積與本月花費計算使用。
- `incomeExpenseMetricsV13`：最後定義為 Version 100，輸出 3／3／3 收入與花費卡片。
- `annualComparisonBaseMonthV101`、`growthMarkupV3`：Version 101，年度比較由每年 1 月基準延伸至次年 1 月。
- `averageExpenseTrendV39`：最後定義為 Version 94，提供平均與本月花費序列。
- `renderAverageExpenseChartV39`、`renderMonthlyExpenseChartV95`：Version 95。
- `scrollToPageTopV96`：左上角品牌快速回頁首。
- `renderRecordSelector`、`initMonthlyRecordActionsV54`：月度資料填寫的月份載入與刪除入口。

### 月度資料卡按鈕殘留

`renderRecord` 仍建立 `.record-actions` 中的載入／刪除按鈕，但 CSS 使用 `.record-actions{display:none!important}` 隱藏。實際入口已移至「每月資料填寫」。除非使用者明確要求，請勿恢復月度資料卡上的按鈕。

## 6. 目前產品基準

- 六個分頁：總覽、月度資料卡、動態回顧、每月資料填寫、賣出紀錄與損益、使用說明。
- 總覽與月度資料卡的「收入與花費」第一列為本月入帳薪水、累積薪水、平均月收入；第二、三列分別為粗估與實際花費，採 3／3／3 排列。
- 總覽與月度資料卡均使用「投資組合指標」名稱。
- 月度資料卡各主要內容為獨立視覺區塊。
- 所有初始資產計算都取該月勾選銀行帳戶的金額；該月未勾選時為 0。
- 月度資料卡年度比較：2 月至 12 月以當年 1 月為基準，1 月以前一年 1 月為基準；年度薪資統計仍按曆年累計。
- 一般數字維持一致深色；只有損益等具方向意義的狀態使用顏色。
- 桌面與手機共用頁面骨架；手機導覽可水平捲動。
- 手機圖表目前可顯示，但多序列圖仍偏窄；使用者已決定暫不調整。
- 最上方工具列固定；左側導覽不固定。

完整公式、Y 軸規則與圖表順序請以 `目前版本說明.md` 為準。

## 7. 資料與備份

Local Storage 鍵：

- `finance-compass-v2`
- `finance-compass-v1`（舊版相容）
- `finance-compass-hide-amounts`

匯出檔名為 `finance-dashboard-YYYY-MM-DD.json`。主要資料結構包含 `current`、`snapshots`、`trades`；陣列欄位即使只有一筆也必須維持陣列。

專案不內附 JSON 測試、真實備份或 Excel 輸入範本。回歸測試請自行建立匿名 JSON。

## 8. 建議修改流程

1. 使用 `rg` 找到相關 HTML、樣式及所有同名函式。
2. 判斷最後生效函式，確認需求是否只限桌面、手機、總覽或動態回顧。
3. 做最小範圍修改，不碰無關資料與樣式。
4. 更新程式內「使用說明」（若公式、操作或名稱改變）。
5. 更新 `目前版本說明.md`；若影響接手方式，再更新本手冊。
6. 執行語法與結構檢查。
7. 以匿名測試 JSON 做對應頁面的回歸測試。

## 9. 基本驗證命令

JavaScript 語法：

```powershell
& 'C:\Users\User\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' --check app.js
```

尋找最後生效函式：

```powershell
rg -n "^function 函式名稱" app.js
```

檢查近期覆寫版本：

```powershell
rg -n "Version (9[0-9]|100)" app.js styles.css
```

目前沒有自動化測試套件；語法檢查後仍必須做瀏覽器與手機實機回歸。

## 10. 最低回歸範圍

- 匯入測試 JSON 成功，資料卡與交易筆數正確。
- 總覽固定引用最新月度資料卡，沒有不存在的未來月份。
- 月度資料卡切換月份後，統計、收入花費、投資組合指標與原始清單一致。
- 初始資產未勾選時為 0；粗估／實際累積花費與本月差額正確。
- 總覽各圖例可開關，Y 軸只依可見資料縮放，端點與右側座標軸不被裁切。
- 動態回顧播放、速度、進度與圖例鎖定正常；沒有明確需求時不要順帶修改。
- 手機版每月填寫及賣出紀錄不重疊；賣出日期與一般輸入框同高。
- 隱藏金額能遮罩數字、座標軸與提示框。
- 左上角品牌可回頁首，最上方工具列持續固定。

## 11. 完成交付格式

交付時簡要說明：

- 完成了什麼。
- 影響哪些分頁及桌面／手機版本。
- 執行了哪些驗證。
- 尚未驗證或需要使用者實機確認的項目。
- 提供已修改檔案的可點擊路徑。

若只完成分析而使用者要求「先不要修改」，明確說明未寫入任何變更。

### 年度資料卡（Version 102）

- HTML 容器：`#annual-records`；年份選單：`#annualRecordYear`；結果容器：`#annualRecordCard`。
- 主要函式：`annualRecordYearsV102()`、`annualExpenseTotalsV102()`、`renderAnnualRecordSelectorV102()`、`renderAnnualRecordV102()`。
- 年末資產採選定年度最後一張月度資料；年度收入依該年度資料加總；年度花費依各月相對前期的差額加總；年度資產變化比較該年度首尾資料。
- 儲存新月度資料與進入年度分頁時會重建年份選單；請保留沒有完整 12 個月時的涵蓋月份與資料筆數提示。
## Version 104 年度系列圖表

- `annualDashboardRowsV104()` 將月度資料依年份分組；資產類取該年最後一張資料，年度流量採該年加總。
- `renderAnnualDashboardChartsV104()` 建立總覽五張年度圖表。
- 年度圖表的 canvas ID 依序為 `annualRatioChart`、`annualHistoryChart`、`annualCumulativeExpenseChart`、`annualAverageExpenseChart`、`annualExpenseChart`。
- `dashboardYAxisBoundsV43()` 的最後有效版本同時處理年度與月度圖表；新增或改名 canvas 時必須同步檢查此函式。
## Version 105 年度大分類熱力圖

- `annualCategoryHeatmapV105(snapshot)` 將年度最後一張月度資料卡彙整為七個大分類，並重用 `heatmapLayoutV20()` 與 `setupHeatmapTooltipsV16()`。
- 年度熱力圖不使用月度熱力圖的逐筆項目與篩選狀態；修改時勿影響 `heatmapV15()`。
## Version 106 年度資產變化

- `annualGrowthMarkupV106(snapshot)` 產生年度資料卡九個成長／績效指標，公式對齊 `growthMarkupV3()` 的年度部分。
- `.annual-growth-grid` 在非手機版固定三欄，手機版單欄；指標順序不可任意調換。
- 年度比較缺少 `annualComparisonBaseMonthV101()` 指定的 1 月資料時，相關卡片維持顯示「—」。
## Version 107 次年 1 月結算

- `annualPeriodV107(year)` 是年度資料卡與年度系列圖表的共同期間來源。
- `baseline`＝N/01；`closing`＝N+1/01，若不存在則採 N 年目前最新資料；`settled` 表示是否已有正式次年 1 月結算資料。
- `salaryItems`＝N/01～N/12；`flowItems`＝N/02～N+1/01。請勿將次年 1 月薪水放入前一年度，但該月花費差額屬於前一年度。
- `annualDashboardRowsV104()` 的最後有效版本已改用此期間函式；調整年度口徑時必須同步測試年度資料卡和總覽年度圖表。
## Version 108 年度熱力圖圖例

- `annualCategoryHeatmapV105()` 的最後有效版本輸出 `data-annual-heatmap-type` 按鈕，並使用獨立的 `annualHeatmapFiltersV108` 集合。
- `setupHeatmapTooltipsV16()` 的最後有效版本同時處理月度／動態回顧的 `data-heatmap-type` 與年度的 `data-annual-heatmap-type`；年度开關只呼叫 `renderAnnualRecordV102()`。
- 年度與月度圖例共用 `.heatmap-legend-item` 樣式，請勿另設年度尺寸覆寫。